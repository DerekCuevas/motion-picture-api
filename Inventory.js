import _ from 'lodash';

export default class Inventory {
    constructor() {
        this.movies = [];
        this.queryCache = {};
    }

    save() {
    }

    filter(genres = [], category = '', fuzzy = '') {
    }

    pageinate(set, limit, offset) {
    }

    total() {
    }

    search(id, offset = 0, limit = 10, genres, category, fuzzy) {
    }

    getMovie(sku) {
    }

    setMovie() {
    }
}
